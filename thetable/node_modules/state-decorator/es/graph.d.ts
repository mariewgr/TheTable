export default class Graph {
    private keys;
    private adj;
    constructor(keys: string[]);
    private isCyclicUtil;
    setEdges(sou: string, dest: string[]): void;
    isCyclic(): boolean;
}
